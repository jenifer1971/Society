# ===========================================
# Society (MiroFish Enterprise) Backend Dependencies
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ── Core framework ────────────────────────────────────────────────────────────
flask>=3.0.0
flask-cors>=6.0.0

# ── LLM providers (install all; only the configured one is imported at runtime)
# OpenAI  /  Azure OpenAI  /  Google Gemini (OpenAI-compatible endpoint)  /  Ollama
openai>=1.0.0

# Anthropic Claude
anthropic>=0.28.0

# AWS Bedrock
boto3>=1.34.0

# ── Zep self-hosted memory (Community Edition — no cloud key needed) ──────────
# The zep-python client works with both the cloud and self-hosted server.
zep-python>=2.0.0

# ── OASIS multi-agent simulation ──────────────────────────────────────────────
camel-oasis==0.2.5
camel-ai==0.2.78

# ── File processing ───────────────────────────────────────────────────────────
PyMuPDF>=1.24.0
charset-normalizer>=3.0.0
chardet>=5.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0
